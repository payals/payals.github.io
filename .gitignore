_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/
.DS_Store
.env
.env.*
!.env.example

# OMC runtime state (not specs/plans/design — those are intentionally tracked)
.omc/state/
.omc/sessions/
.omc/project-memory.json
.omo/

# Local drafts and outlines (not for publish)
drafts/
.omx/

# here.now local state / claim tokens — never publish to this public repo
.herenow/

# Operator notes: contains private home-server IPs — keep out of the public repo
CLAUDE.md
AGENTS.md
